
public class Encapsulatoin {

	public static void main(String[] args) {
		
		Student student = new Student(); 
		student.setFirstName ("S"); 
		student.setPhoneNumber("12349567890");
		student.intoduce();
		/*
		 * Access Modifiers;
		 * public 	accessible everywhere
		 * private	only accessible within the class itself
		 * protected  accessible within the class, other classes in the same package, and all subclasses 
		 * No Modifier same as protected, except it is not accessible in a different packages. 
		 */
		
		
		Rectangle rec = new Rectangle(10.0, 15.0); 
		rec.setLength(10.0
				00;
		System.out.println(rec.getArea());
		
		
	}

}
